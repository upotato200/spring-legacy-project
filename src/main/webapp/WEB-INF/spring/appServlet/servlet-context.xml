<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<annotation-driven/>

	<resources mapping="/resources/**" location="/resources/"/>

	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/"/>
		<beans:property name="suffix" value=".jsp"/>
	</beans:bean>

	<!-- 파일 업로드 (최대 30MB) -->
	<beans:bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<beans:property name="maxUploadSize" value="31457280"/>
		<beans:property name="defaultEncoding" value="UTF-8"/>
	</beans:bean>

	<!-- 파일 저장 경로: 사용자 홈 하위 (C:\ 직접 접근 시 권한 오류 방지) -->
	<beans:bean id="uploadPath" class="java.lang.String">
		<beans:constructor-arg value="#{systemProperties['user.home']}/uploadFiles"/>
	</beans:bean>

	<!-- 기존 인터셉터 (레거시 게시판용) -->
	<beans:bean id="sampleInterceptor"  class="com.jh.interceptor.SampleInterceptor"/>
	<beans:bean id="authInterceptor"    class="com.jh.interceptor.AuthInterceptor"/>
	<beans:bean id="loginInterceptor"   class="com.jh.interceptor.LoginInterceptor"/>

	<interceptors>
		<interceptor>
			<mapping path="/doA"/>
			<mapping path="/doB"/>
			<beans:ref bean="sampleInterceptor"/>
		</interceptor>
		<!-- /user/loginPost 는 Spring Security 가 처리하므로 LoginInterceptor 제거 -->
		<interceptor>
			<mapping path="/sboard/register"/>
			<mapping path="/sboard/modifyPage"/>
			<mapping path="/sboard/removePage"/>
			<beans:ref bean="authInterceptor"/>
		</interceptor>
	</interceptors>

	<!-- 컨트롤러 스캔: 기존 + 신규 -->
	<context:component-scan base-package="com.jh.controller"/>

</beans:beans>
